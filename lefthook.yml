# Format and lint
pre-commit:
  commands:
    check:
      run: npm run check:fix --no-errors-on-unmatched

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npm run commitlint -- --edit {1}