# Format and lint
pre-commit:
  commands:
    lint:
      run: npm run lint
      stage_fixed: true

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npm run commitlint -- --edit {1}